# signal-trader-demo — autopilot log

One line per iteration. Newest last.

- 2026-06-26 — Gate already fully green (218→242 tests, ruff clean); the prompt's "~196/218" was stale. Built the FREE synthetic-delisting survivorship stress test (Phase 4 headline): `survivorship.py` (point-in-time haircut + `delisting_mask`), `delistings.py` (SEC Form 25/25-NSE FTS, offline-first cache, network behind injected seam, truncation warning), `evaluate_ml` opt-in + CLI `--survivorship-stress`/`--delisting-haircut`, `ingest_delistings.py`. Measured on local 108-name cache (252 OOS reb): survivors-only ML loses to momentum (diff-PSR 0.10); under −0.40/−0.60/−1.00 haircut ML "beats" baseline (diff-PSR 1.0) — but the honest reading is BASELINE FRAGILITY (baseline picks shaded decliners 31.5% vs ML 3.3%, now emitted by the scorecard), both lose money, NOT an ML edge. Methodology review (subagent) found one should-fix: shift-test sample now gated on the pre-haircut formed mask (shading changes values, not which rebalances qualify). README + Phase-4 plan updated with the artifact + honest limits (paid CRSP = Needs Nico). On branch feat/survivorship-stress-test.
  - Needs Nico (deferred, NOT fired autonomously — both are stateful network actions; keys ARE present in .env): (1) live Alpaca PAPER smoke for the Phase-3 forward loop (`run_forward_paper.py`) — architecture/tests complete, only the live order round-trip remains; (2) live SEC delisting fetch (`ingest_delistings.py`, SEC_IDENTITY present) to populate `data/delistings.csv` with the real Form 25/25-NSE list (the stress test ran on real decliner names from the ml_cache, not the live SEC list).
- 2026-07-01 — First-iteration reconciliation (LOOP.md step 3): `PROJECT.md`'s status header was stale ("Phase 0 -> Phase 1", 2026-06-17) while README/commits already show Phases 1-4 done. Updated status line, roadmap checkmarks, and cross-links to README/plans/this log; no code change, gate was already green (242 tests, ruff clean). Next: scan for further open work now that state is accurate.
- 2026-07-01 — Resolved the stale `§Open inputs` checklist in `PROJECT.md` (Alpaca keys/SEC_IDENTITY marked present per the 2026-06-26 log entry, but live round-trips stay deferred under "Needs Nico"; git remote/visibility closed as local-only per `AUTOPILOT.md`'s register; Tiingo remains genuinely open, non-blocking).
- 2026-07-01 — Implemented Fix 4 (non-overlapping rebalancing), the one explicitly-flagged-but-undone gap in the Phase-4 plan doc: `evaluate_ml(..., non_overlapping=True)` strides OOS test dates by `horizon` bars so consecutive rebalances never share a holding period, making absolute PSR/Sharpe trustworthy (opt-in, default OFF, same pattern as `--consensus`/`--survivorship-stress`). Wired through `scripts/run_ml_experiment.py --non-overlapping`; verified live against the offline cache (126 overlapping reb. -> 26 non-overlapping, ~n/horizon as expected, no network call). 2 new tests (244 total), ruff clean. Self-reviewed against the `backtest-methodology-reviewer` checklist (no subagent-dispatch tool available in this run, so applied it manually): no new leakage/survivorship/cost vector; documented one honest limit — the stride phase is fixed at each fold's first test date (not randomized), applied identically to ML/baseline so the margin stays fair but not stress-tested across phase offsets. Plan doc updated to mark Fix 4 landed. README's honest-harness section updated to mention the new opt-in. Considered re-running the consensus A/B with `--non-overlapping` for a fresher measurement, but the original run's exact params (universe/window) aren't recorded in the plan doc — guessing them and presenting a "re-run" would risk a non-comparable, effectively fabricated number, so left as a documented follow-up instead (plan doc already says "not re-run for this consensus A/B").
- 2026-07-01 — Audited for further open work: frontend (`npm run build`/`npm run test`) green, not otherwise gated by LOOP.md but checked as due diligence since Phase 3 claims "done"; `signal-trader_v7_kit.html` confirmed as the (still-referenced) design source already ported into `frontend/`, not orphaned; scanned for TODO/FIXME (none) and modules lacking dedicated tests (only `backtest/result.py`'s one-line `returns()` and a Protocol/dataclass file — both low-value, not pursued this run) and the "Fix 4" M-effort follow-up on `train_ml_retro.py` (out of scope — that script doesn't use the existing consensus/survivorship opt-in pattern either). No further Nico-unblocked, in-scope, high-value task found; stopping here for this run rather than manufacturing busywork.
- 2026-07-02 — Portfolio-polish session (7 tasks, all landed, 244→280 tests, gate green throughout): (1) README `## Setup` replaced the stale "wird in Phase 0 etabliert" placeholder with a verified clone-to-first-report quickstart (`uv sync` -> `backfill.py` -> `run_backtest.py`, actual output included). (2) Removed the drifting hardcoded test counts (README 238 / PROJECT.md 242 vs real 244) from both docs' prose, pointed at `uv run pytest --collect-only | tail -1` instead — no number left to manually maintain. (3) `config.DEFAULT_END` was a hard-coded "2026-01-01" that had already slipped into the past, silently truncating every CLI's default `--end`; now `dt.date.today().isoformat()` (PIT-safe: only widens/narrows the default fetch window, never a signal's `timestamp_known`), + regression test. (4) Added the Deflated Sharpe Ratio (Bailey & López de Prado 2014) next to PSR in `metrics.py`, independently re-derived and tested against the paper's own worked example (SR=2.5 ann., Var[SR]=0.5, 100 trials, 1250d, skew=-3, kurt=10 -> DSR~0.8997); replaced the never-actually-populated `evaluate_ml(n_configs_tested=...)` knob with `backtest/trial_log.py` — an append-only local trial history that `run_backtest.py`/`run_ml_experiment.py`/`generate_tearsheet.py` write to on every run, so DSR is computed from the REAL trial count, not a guess. Both CLIs now print `DSR=` next to `PSR=` and the honest trial count. (5) Built an HTML tearsheet: `quantstats-reloaded`'s own `reports.html()` turned out broken against this repo's pinned pandas (`ValueError` in every mode, reproduced with synthetic data) so `backtest/tearsheet.py` builds the same content directly with matplotlib (equity curve, drawdown, monthly heatmap in the dataviz-skill's validated blue/red diverging palette — not the usual green/red, which is a poor pairing for red-green color blindness) + the honest metric set incl. PSR/DSR + cost disclosure, as one self-contained HTML file (no CDN/JS). `scripts/generate_tearsheet.py` always attaches the repo's sober findings (e.g. ML not beating the baseline) regardless of the shown run's own numbers. New dep: `matplotlib==3.11.0`, promoted from an already-locked transitive dep, no new download. (6) Removed inert `# noqa: BLE001` comments (BLE is not in ruff's select list, so these never did anything) across 10 files, kept the rationale text; moved `signal-trader_v7_kit.html` from repo root to `docs/design/`, fixed the one remaining reference (`frontend/README.md`). (7) Added `backtest/pbo.py`: Probability of Backtest Overfitting via Combinatorially Symmetric Cross-Validation (Bailey et al. 2015) — vectorized on numpy inside the CSCV loop (a naive pandas-per-combination version cost ~50s at the paper's 16-block default; this costs low single digits), tested against the paper's own qualitative properties (PBO~0.5 for indistinguishable strategies, PBO~0 for one with a real edge). Standalone module, not yet wired into a CLI (would need a multi-config sweep runner the existing single-strategy scripts don't have) — documented follow-up. 12 commits total on `autopilot/work`, all atomic, all green-gated.
